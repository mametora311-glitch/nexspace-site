name: nexspace_site
description: Nexspace公式トップページ（GitHub Pages対応）
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
  assets:
    - assets/images/nexspace_logo.png
